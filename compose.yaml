services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=next-step-education-db'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=next-step'
    ports:
      - '7654:5432'
